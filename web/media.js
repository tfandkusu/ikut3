function getMediaDeviceInfoDeviceId(mediaDeviceInfo) {
    return mediaDeviceInfo.deviceId;
}

function getMediaDeviceInfoKind(mediaDeviceInfo) {
    return mediaDeviceInfo.kind;
}

function getMediaDeviceInfoLabel(mediaDeviceInfo) {
    return mediaDeviceInfo.label;
}
